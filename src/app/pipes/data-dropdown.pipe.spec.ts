import { DataDropdownPipe } from './data-dropdown.pipe';

describe('DataDropdownPipe', () => {
  it('create an instance', () => {
    const pipe = new DataDropdownPipe();
    expect(pipe).toBeTruthy();
  });
});
